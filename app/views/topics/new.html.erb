<div class="topic-new-wrapper" >
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <h1 class="text-center">募集をかける</h1>
        <%= form_for @topic do |f| %>
          <div class="form-group">
            <%= f.label :title %>
            <%= f.text_area :title, class: 'form-control', placeholder: "valolant ダイア帯@2 ディスコID" %>
          </div>

          <div class="form-group">
            <%= f.label :body %>
            <%= f.text_area :body, class: 'form-control', placeholder: "詳細を記入してください" %>
          </div>
         <%if logged_in?%>
          <%= f.submit '投稿', class: 'btn btn-black btn-block' %>
         <% end %>
        <% end %>
        <p>※タイトルにゲーム名を表記してください</p>
      </div>
    </div>  
  </div>
</div>